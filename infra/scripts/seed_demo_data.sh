#!/usr/bin/env bash
set -euo pipefail

echo "Demo fixture data is file-backed at fixtures/demo_catalog.json."
echo "PostgreSQL seed wiring is deferred; APIs run in deterministic fixture mode for the demo gateway."
