# Artifact Refresh Worker

VM1-owned scaffold for polling `Real_service/<version>/manifest.json`, validating bundle completeness, staging files locally, and requesting a serving restart.

Hot reload is intentionally out of scope for the MVP.
